@{
    ViewData["Title"] = "Home Page";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OmanPharmaAcademy - Modern & Complete Homepage</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0a0a0a;
            color: #e2e8f0;
        }
        /* --- Preloader Styles --- */
        #preloader {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #0a0a0a;
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: opacity 0.75s ease, visibility 0.75s ease;
        }

        .atom-spinner {
            height: 60px;
            width: 60px;
            overflow: hidden;
        }

            .atom-spinner .spinner-inner {
                position: relative;
                display: block;
                height: 100%;
                width: 100%;
            }

            .atom-spinner .spinner-line {
                position: absolute;
                width: 100%;
                height: 100%;
                border-radius: 50%;
                animation-duration: 1s;
                border-left-width: calc(60px / 25);
                border-top-width: calc(60px / 25);
                border-left-color: #8b5cf6; /* New Purple Color */
                border-left-style: solid;
                border-top-style: solid;
                border-top-color: transparent;
                animation: atom-spinner-animation 1s linear infinite;
            }

                .atom-spinner .spinner-line:nth-child(1) {
                    animation-delay: calc(1s * -0.15);
                    transform: rotateZ(20deg) rotateX(66deg) rotateY(0deg);
                }

                .atom-spinner .spinner-line:nth-child(2) {
                    animation-delay: calc(1s * -0.3);
                    transform: rotateZ(40deg) rotateX(66deg) rotateY(0deg);
                }

                .atom-spinner .spinner-line:nth-child(3) {
                    animation-delay: calc(1s * -0.45);
                    transform: rotateZ(60deg) rotateX(66deg) rotateY(0deg);
                }

        @@keyframes atom-spinner-animation {
            100% {
                transform: rotateZ(360deg) rotateX(66deg) rotateY(0deg);
            }
        }

        @@keyframes glowing-border {
            0% {
                box-shadow: 0 0 5px #8b5cf6, 0 0 10px #8b5cf6, 0 0 15px #a78bfa;
            }

            50% {
                box-shadow: 0 0 10px #8b5cf6, 0 0 20px #8b5cf6, 0 0 30px #a78bfa;
            }

            100% {
                box-shadow: 0 0 5px #8b5cf6, 0 0 10px #8b5cf6, 0 0 15px #a78bfa;
            }
        }

        .glowing-card {
            animation: glowing-border 2s infinite ease-in-out;
        }

        .prose-invert ul {
            list-style-type: disc;
            margin-left: 1.5rem;
        }

        .prose-invert ol {
            list-style-type: decimal;
            margin-left: 1.5rem;
        }

        .prose-invert a {
            color: #60a5fa;
        }

        .prose-invert strong {
            color: #fff;
        }

        @@keyframes liquid-background {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        .liquid-glass {
            background-color: rgba(255, 255, 255, 0.05);
            background-image: linear-gradient(110deg, rgba(255, 255, 255, 0.1) 0%, rgba(255, 255, 255, 0) 25%, rgba(255, 255, 255, 0.1) 50%, rgba(255, 255, 255, 0) 75%, rgba(255, 255, 255, 0.1) 100%);
            background-size: 200% 200%;
            animation: liquid-background 15s ease infinite;
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
    </style>
</head>
<body class="antialiased">

    <!-- Preloader -->
    <div id="preloader">
        <div class="atom-spinner">
            <div class="spinner-inner">
                <div class="spinner-line"></div>
                <div class="spinner-line"></div>
                <div class="spinner-line"></div>
            </div>
        </div>
    </div>

    <!-- Header -->
    <header id="header" class="fixed top-0 left-0 right-0 z-50 transition-all duration-300 liquid-glass">
        <div class="container mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <div class="text-2xl font-bold">
                    <a href="#">OmanPharmaAcademy</a>
                </div>
                <nav class="hidden lg:flex items-center space-x-8">
                    <a href="#courses" class="hover:text-purple-400 transition-colors">Courses</a>
                    <a href="#about" class="hover:text-purple-400 transition-colors">About</a>
                    <a href="#features" class="hover:text-purple-400 transition-colors">Features</a>
                    <a href="#contact" class="hover:text-purple-400 transition-colors">Contact</a>
                </nav>
                <div class="hidden lg:flex items-center gap-4">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <div class="hidden lg:block relative">
                            <button id="user-menu-button" class="flex items-center gap-2 font-semibold hover:text-purple-400 transition-colors">
                                <span>Welcome, @User.FindFirst("FirstName")?.Value</span>
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                            </button>
                            <!-- Dropdown Panel -->
                            <div id="user-menu" class="absolute right-0 mt-3 w-48 rounded-xl shadow-lg liquid-glass hidden z-50 overflow-hidden">
                                <div class="py-1">
                                    <a asp-area="User" asp-controller="Dashboard" asp-action="Index" class="block px-4 py-3 text-sm text-slate-300 hover:bg-purple-500/20 hover:text-white transition-colors">Dashboard</a>
                                    <a asp-area="User" asp-controller="Profile" asp-action="EditProfile" class="block px-4 py-3 text-sm text-slate-300 hover:bg-purple-500/20 hover:text-white transition-colors">Profile</a>
                                    <a asp-area="" asp-controller="Account" asp-action="Logout" class="block px-4 py-3 text-sm text-red-400 hover:bg-red-500/20 hover:text-red-300 transition-colors">Logout</a>
                                </div>
                            </div>
                        </div>
                        @* <a asp-area="" asp-controller="Account" asp-action="Login" class="font-semibold hover:text-purple-400 transition-colors">
                            Welcome Back, @User.FindFirst("FirstName")?.Value
                        </a> *@
                    }
                    else
                    {
                        <a asp-area="" asp-controller="Account" asp-action="Login" class="font-semibold hover:text-purple-400 transition-colors">
                            Login
                        </a>
                        <a asp-area="" asp-controller="Account" asp-action="Register" class="bg-purple-500 hover:bg-purple-600 text-white font-bold py-2 px-6 rounded-full transition-transform hover:scale-105">
                            Sign Up
                        </a>
                    }
                </div>
                <div class="lg:hidden">
                    <button id="mobile-menu-button" class="text-slate-300 hover:text-purple-400">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path></svg>
                    </button>
                </div>
            </div>
        </div>
    </header>

    <!-- Offcanvas Menu -->
    <div id="offcanvas-backdrop" class="fixed inset-0 bg-black/70 z-40 hidden opacity-0 transition-opacity duration-300 ease-in-out"></div>
    <div id="offcanvas-menu" class="fixed top-0 right-0 h-full w-80 max-w-[85%] z-50 transform translate-x-full transition-transform duration-300 ease-in-out liquid-glass flex flex-col">
        <!-- Header -->
        <div class="flex justify-between items-center p-4 border-b border-slate-700/50">
            <a href="#" class="text-xl font-bold">NextPharmacists</a>
            <button id="offcanvas-close-button" class="text-slate-300 hover:text-purple-400">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
            </button>
        </div>
        <!-- Body -->
        <nav class="flex-grow p-4 flex flex-col justify-center text-center">
            @if (User.Identity.IsAuthenticated)
            {
                <div class="text-lg font-medium text-purple-400 mb-6">Welcome, @User.FindFirst("FirstName")?.Value</div>
            }
            <div class="space-y-2">
                <a href="#courses" class="block text-lg font-medium py-3 px-4 rounded-full hover:bg-white/10 transition-colors">Courses</a>
                <a href="#about" class="block text-lg font-medium py-3 px-4 rounded-full hover:bg-white/10 transition-colors">About</a>
                <a href="#features" class="block text-lg font-medium py-3 px-4 rounded-full hover:bg-white/10 transition-colors">Features</a>
                <a href="#contact" class="block text-lg font-medium py-3 px-4 rounded-full hover:bg-white/10 transition-colors">Contact</a>
            </div>
        </nav>
        <!-- Footer -->
       @if (User.Identity.IsAuthenticated)
        {
            <div class="p-4 mt-auto space-y-3">
                <a asp-area="User" asp-controller="Dashboard" asp-action="Index" class="w-full block text-center bg-purple-500 hover:bg-purple-600 text-white font-bold py-3 px-6 rounded-full transition-colors">
                    Dashboard
                </a>
                <a asp-area="" asp-controller="Account" asp-action="Logout" class="w-full block text-center bg-red-600/50 hover:bg-red-600/70 text-white font-bold py-3 px-6 rounded-full transition-colors">
                    Logout
                </a>
            </div>
        }
       else
       {
           <div class="p-4 mt-auto space-y-3">
                <a asp-area="" asp-controller="Account" asp-action="Login" class="w-full block text-center bg-slate-700/50 hover:bg-slate-700 text-white font-bold py-3 px-6 rounded-full transition-colors">
                   Login
               </a>
                <a asp-area="" asp-controller="Account" asp-action="Register" class="w-full block text-center bg-purple-500 hover:bg-purple-600 text-white font-bold py-3 px-6 rounded-full transition-transform hover:scale-105">
                    Sign Up
               </a>
           </div>
       }
    </div>


    <main>
        <!-- Hero Section -->
        <section class="min-h-screen flex items-center pt-24 pb-12 bg-cover bg-center" style="background-image: radial-gradient(circle at top left, rgba(6, 182, 212, 0.2), transparent 40%), radial-gradient(circle at bottom right, rgba(139, 92, 246, 0.2), transparent 40%);">
            <div class="container mx-auto px-4 text-center">
                <h1 class="text-4xl md:text-6xl font-extrabold text-slate-100 mb-6 leading-tight">
                    Master the <span class="text-purple-400">Pearson VUE</span> Oman Pharmacy Exam
                </h1>
                <p class="text-lg md:text-xl text-slate-400 max-w-3xl mx-auto mb-10">
                    Join OmanPharmaAcademy and get comprehensive training from someone who has successfully passed the OMSB exam. Expert guidance, proven methods, and complete support until you achieve your license.
                </p>
                <a href="#courses" class="bg-purple-500 hover:bg-purple-600 text-white font-bold py-4 px-10 rounded-full text-lg transition-transform hover:scale-105 inline-block">
                    Start Your Journey
                </a>
            </div>
        </section>

        <!-- Courses Section -->
        <section id="courses" class="py-20">
            <div class="container mx-auto px-4">
                <div class="text-center mb-16">
                    <h2 class="text-3xl md:text-4xl font-bold">Choose Your Learning Path</h2>
                    <p class="text-slate-400 mt-4 max-w-2xl mx-auto">Tailored courses designed for different learning needs and experience levels.</p>
                </div>
                <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                    <!-- Crash Course -->
                    <div class="rounded-2xl p-8 transform hover:-translate-y-2 transition-transform duration-300 liquid-glass">
                        <div class="text-3xl font-bold text-cyan-400 mb-4">🎓</div>
                        <h3 class="text-2xl font-bold mb-4">Crash Course</h3>
                        <p class="text-slate-400 mb-6">Complete offline preparation package with everything you need to pass.</p>
                        <ul class="space-y-3 text-slate-300">
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>7 Complete Training Videos</li>
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>Specialized Study Materials</li>
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>Complete Non-Pharma Section</li>
                        </ul>
                        <button class="w-full mt-8 bg-cyan-500 hover:bg-cyan-600 text-slate-900 font-bold py-3 px-6 rounded-full transition-transform hover:scale-105">Enroll Now</button>
                    </div>
                    <!-- 1-to-1 Personal -->
                    <div class="rounded-2xl p-8 transform hover:-translate-y-2 transition-transform duration-300 liquid-glass glowing-card relative border-2 border-purple-500">
                        <div class="absolute top-0 -translate-y-1/2 left-1/2 -translate-x-1/2 bg-purple-400 text-slate-900 text-sm font-bold px-4 py-1 rounded-full">Most Popular</div>
                        <div class="text-3xl font-bold text-purple-400 mb-4">👤</div>
                        <h3 class="text-2xl font-bold mb-4">1-to-1 Personal</h3>
                        <p class="text-slate-400 mb-6">Personalized training with dedicated support and custom planning.</p>
                        <ul class="space-y-3 text-slate-300">
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>Personal Teaching Sessions</li>
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>Custom Study Plan</li>
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>24/7 Support</li>
                        </ul>
                        <button class="w-full mt-8 bg-purple-500 hover:bg-purple-600 text-white font-bold py-3 px-6 rounded-full transition-transform hover:scale-105">Book Consultation</button>
                    </div>
                    <!-- Questions Only -->
                    <div class="rounded-2xl p-8 transform hover:-translate-y-2 transition-transform duration-300 liquid-glass">
                        <div class="text-3xl font-bold text-teal-400 mb-4">🎯</div>
                        <h3 class="text-2xl font-bold mb-4">Questions Only</h3>
                        <p class="text-slate-400 mb-6">Perfect for those with basic knowledge who need focused practice.</p>
                        <ul class="space-y-3 text-slate-300">
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>Frequently Asked Questions</li>
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>Detailed Answer Analysis</li>
                            <li class="flex items-center"><svg class="w-5 h-5 text-green-400 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path></svg>Instant Access</li>
                        </ul>
                        <button class="w-full mt-8 bg-teal-500 hover:bg-teal-600 text-slate-900 font-bold py-3 px-6 rounded-full transition-transform hover:scale-105">Start Practicing</button>
                    </div>
                </div>
            </div>
        </section>

        <!-- Features Section -->
        <section id="features" class="py-20">
            <div class="container mx-auto px-4">
                <div class="text-center mb-16">
                    <h2 class="text-3xl md:text-4xl font-bold">Modern Learning Platform</h2>
                    <p class="text-slate-400 mt-4 max-w-2xl mx-auto">All your training needs in one secure, organized platform.</p>
                </div>
                <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8 text-center">
                    <div class="rounded-2xl p-6 liquid-glass">
                        <div class="text-4xl text-cyan-400 mb-4">🛡️</div>
                        <h3 class="text-xl font-bold">Secure Content</h3>
                    </div>
                    <div class="rounded-2xl p-6 liquid-glass">
                        <div class="text-4xl text-green-400 mb-4">💳</div>
                        <h3 class="text-xl font-bold">Global Payments</h3>
                    </div>
                    <div class="rounded-2xl p-6 liquid-glass">
                        <div class="text-4xl text-teal-400 mb-4">📈</div>
                        <h3 class="text-xl font-bold">Progress Tracking</h3>
                    </div>
                    <div class="rounded-2xl p-6 liquid-glass">
                        <div class="text-4xl text-purple-400 mb-4">📱</div>
                        <h3 class="text-xl font-bold">Anywhere Access</h3>
                    </div>
                </div>
            </div>
        </section>

        <!-- CTA Section -->
        <section id="contact" class="py-20">
            <div class="container mx-auto px-4">
                <div class="rounded-2xl p-12 text-center bg-gradient-to-r from-purple-500 to-indigo-600">
                    <h2 class="text-3xl md:text-4xl font-bold text-white">Ready to Start Your Success Journey?</h2>
                    <p class="text-purple-200 mt-4 max-w-2xl mx-auto">Join over 100 successful pharmacists who achieved their Oman licensing through our proven training programs.</p>
                    <a href="#courses" class="mt-8 bg-white hover:bg-slate-200 text-slate-900 font-bold py-4 px-10 rounded-full text-lg transition-transform hover:scale-105 inline-block">Enroll Now</a>
                </div>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer class="py-12 border-t border-slate-800">
        <div class="container mx-auto px-4 text-center text-slate-400">
            <p>&copy; 2025 OmanPharmaAcademy. All rights reserved.</p>
        </div>
    </footer>


    <script>
        document.addEventListener('DOMContentLoaded', () => {

            // Preloader logic
            const preloader = document.getElementById('preloader');
            window.addEventListener('load', () => {
                setTimeout(() => {
                    preloader.style.opacity = '0';
                    preloader.style.visibility = 'hidden';
                }, 500); // Small delay to ensure animations are smooth
            });

            // Offcanvas menu toggle
            const mobileMenuButton = document.getElementById('mobile-menu-button');
            const offcanvasMenu = document.getElementById('offcanvas-menu');
            const offcanvasBackdrop = document.getElementById('offcanvas-backdrop');
            const offcanvasCloseButton = document.getElementById('offcanvas-close-button');
            const offcanvasLinks = document.querySelectorAll('#offcanvas-menu a');

            function openMenu() {
                document.body.classList.add('overflow-hidden');
                offcanvasBackdrop.classList.remove('hidden');
                setTimeout(() => {
                    offcanvasBackdrop.classList.remove('opacity-0');
                    offcanvasMenu.classList.remove('translate-x-full');
                }, 10);
            }

            function closeMenu() {
                document.body.classList.remove('overflow-hidden');
                offcanvasBackdrop.classList.add('opacity-0');
                offcanvasMenu.classList.add('translate-x-full');
                setTimeout(() => {
                    offcanvasBackdrop.classList.add('hidden');
                }, 300);
            }

            mobileMenuButton.addEventListener('click', openMenu);
            offcanvasCloseButton.addEventListener('click', closeMenu);
            offcanvasBackdrop.addEventListener('click', closeMenu);
            offcanvasLinks.forEach(link => {
                link.addEventListener('click', closeMenu);
            });

            // Header scroll effect
            const header = document.getElementById('header');
            window.addEventListener('scroll', () => {
                if (window.scrollY > 50) {
                    header.classList.add('shadow-lg');
                } else {
                    header.classList.remove('shadow-lg');
                }
            });

            // User dropdown menu
            const userMenuButton = document.getElementById('user-menu-button');
            const userMenu = document.getElementById('user-menu');

            if (userMenuButton) {
                userMenuButton.addEventListener('click', (e) => {
                    e.stopPropagation(); // Prevent window click from closing it immediately
                    userMenu.classList.toggle('hidden');
                });
            }

            // Close dropdown when clicking outside
            window.addEventListener('click', (e) => {
                if (userMenu && !userMenu.classList.contains('hidden')) {
                    if (!userMenu.contains(e.target) && !userMenuButton.contains(e.target)) {
                        userMenu.classList.add('hidden');
                    }
                }
            });


        });
    </script>
</body>
</html>

